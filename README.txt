How to compile this code:
          	This cpp file can be compiled in any environment support C++11. 
	Building a c++ project in a IDE and then add all files in src to the project can simply compile it.
	This program has been tested in Windows 10.

How to run this code:
	After compiling, run the executable file. Name of input file should be the command line argument(inputfilename.asm). Input file name 
can be in both absolute path and relative path.

What works and what doesn't work:
	All .asm files passed.
	
	
 

